class Parent
{
	private int x; //seen inside the class only (this is the default)
	protected int y; //seen inside the class and the child classes (sub-classes)
	public int z; //seen everywhere in the project
	
	/* same access modifiers for properties and methods */
}

class Child : Parent
{
}
